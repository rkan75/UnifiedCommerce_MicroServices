# Required: secret for signing/validating JWTs. Use at least 32 characters (e.g. openssl rand -hex 32).
# Without this, registration and login will fail with 503.
JWT_SECRET=43c165c91c29d4762ea3e3171056e933d65fc7381a3cf8036f7a126cebf3e1fb

# Database (same as Medusa / store backend)
SPRING_DATASOURCE_URL=jdbc:postgresql://127.0.0.1:5433/grocery_store
SPRING_DATASOURCE_USERNAME=grocery_app
SPRING_DATASOURCE_PASSWORD=your_password

# Optional overrides
# SERVER_PORT=8087
# CUSTOMER_TABLE=customer
# ADDRESS_TABLE=address
# AUTH_TABLE=customer_auth
# JWT_EXPIRES_SECONDS=604800
