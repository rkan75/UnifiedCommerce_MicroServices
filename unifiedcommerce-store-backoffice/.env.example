# Backend API base URL (Medusa admin). Required for production or when not using Vite proxy.
# In dev with default Vite proxy, leave unset to use relative /admin requests (proxied to backend).
# VITE_MEDUSA_BACKEND_URL=http://localhost:9000
