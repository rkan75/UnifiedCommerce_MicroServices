# Public static assets

Place **tcslogo.png** here for the admin welcome/login screen.

The backend serves it at `GET /admin/assets/tcslogo.png`, and the dashboard patch uses this URL for the logo on the login and invite pages.
