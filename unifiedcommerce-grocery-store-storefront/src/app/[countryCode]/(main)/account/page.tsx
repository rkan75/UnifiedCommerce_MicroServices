/**
 * Root account segment page. Renders nothing; the account layout
 * shows either the dashboard or login slot based on auth state.
 * This page is required so /us/account is a valid route.
 */
export default function AccountPage() {
  return null
}
